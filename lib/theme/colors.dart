import 'package:flutter/material.dart';

/// Vitala palette: deep medical teal over mist white.
abstract class VitalaColors {
  static const mist = Color(0xFFF7FAF9); // background
  static const teal = Color(0xFF0F766E); // primary (buttons, emphasis)
  static const tealDark = Color(0xFF0B5D57); // pressed/hover
  static const tealSoft = Color(0xFFDCEFEC); // tinted surfaces, chips
  static const ink = Color(0xFF1A2E2B); // text
  static const inkSoft = Color(0xFF5C7370); // secondary text
  static const surface = Color(0xFFFFFFFF); // cards
  static const line = Color(0xFFE2ECEA); // borders
  static const danger = Color(0xFFD9534F); // end call
}
